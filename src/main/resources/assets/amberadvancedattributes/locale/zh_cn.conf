attributes {
  attack {
    fixed = "&c攻击 &a{0}"
    range = "&c攻击 &a{0} - &a{1}"
  }
  tracing {
    fixed = "&3追踪 &a{0}"
    range = "&3追踪 &a{0} - &a{1}"
  }
  possession {
    none = "无人"
    lore = "&e{0} &e拥有此物品"
  }
}