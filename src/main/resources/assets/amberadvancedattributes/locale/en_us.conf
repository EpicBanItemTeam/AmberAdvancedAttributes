attributes {
  tracing {
    fixed = "&3Tracing &a{0}"
    range = "&3Tracing &a{0} - &a{1}"
  }
  accelerate {
    fixed = "&3Arrow Speed &a{0}"
    range = "&3Arrow Speed &a{0} - &a{1}"
  }
  possession {
    none = "No Possession"
    lore = "&e{0} &epossess this item"
    no-permission = "It is not permitted since the item is possessed by another player"
  }
  attack {
    fixed = "&eAttack Damage &c{0}"
    range = "&eAttack Damage &c{0} - &c{1}"
  }
  pvp-attack {
    fixed = "&ePVP Attack Damage &c{0}"
    range = "&ePVP Attack Damage &c{0} - &c{1}"
  }
  pve-attack {
    fixed = "&ePVE Attack Damage &c{0}"
    range = "&ePVE Attack Damage &c{0} - &c{1}"
  }
  defense {
    fixed = "&eDefence &b{0}"
    range = "&eDefence &b{0} - &b{1}"
  }
  pvp-defense {
    fixed = "&ePVP Defence &b{0}"
    range = "&ePVP Defence &b{0} - &b{1}"
  }
  pve-defense {
    fixed = "&ePVE Defence &b{0}"
    range = "&ePVE Defence &b{0} - &b{1}"
  }
  reflect {
    fixed = "&cDamage Reflection &a{0}"
    range = "&cDamage Reflection &a{0} - &a{1}"
  }
  pvp-reflect {
    fixed = "&cPVP Damage Reflection &a{0}"
    range = "&cPVP Damage Reflection &a{0} - &a{1}"
  }
  pve-reflect {
    fixed = "&cPVE Damage Reflection &a{0}"
    range = "&cPVE Damage Reflection &a{0} - &a{1}"
  }
  reflect-rate {
    fixed = "&cReflection Rate &a{0}"
    range = "&cReflection Rate &a{0} - &a{1}"
  }
  critical {
    fixed = "&dCritical Hit Damage &a{0}"
    range = "&dCritical Hit Damage &a{0} - &a{1}"
  }
  critical-rate {
    fixed = "&dCritical Hit Rate &a{0}"
    range = "&dCritical Hit Rate &a{0} - &a{1}"
  }
  dodge {
    fixed = "&6Dodge Rate &a{0}"
    range = "&6Dodge Rate &a{0} - &a{1}"
  }
  accuracy {
    fixed = "&6Hit Accuracy &a{0}"
    range = "&6Hit Accuracy &a{0} - &a{1}"
  }
  attack-speed {
    fixed = "&6Attack Speed &a{0}"
    range = "&6Attack Speed &a{0} - &a{1}"
  }
  move-speed {
    fixed = "&6Move Speed &a{0}"
    range = "&6Move Speed &a{0} - &a{1}"
  }
  burn {
    fixed = "&3Burn Time &a{0}t"
    range = "&3Burn Time &a{0}t - &a{1}t"
  }
  burn-rate {
    fixed = "&3Burn Rate &a{0}"
    range = "&3Burn Rate &a{0} - &a{1}"
  }
  durability {
    value = "&bDurability &a{0}&f/ &a{1}"
  }
  unbreakable {
    value = "&cUnbreakable"
  }
  instant-death-immune {
    value = "&cImmune to Instant Death"
  }
  instant-death {
    fixed = "&3Instant Death Rate &a{0}"
    range = "&3Instant Death Rate &a{0} - &a{1}"
  }
  knockback {
    fixed = "&3Knockback &a{0}"
    range = "&3Knockback &a{0} - &a{1}"
  }
  regeneration {
    fixed = "&cRegeneration &a{0}"
    range = "&cRegeneration &a{0} - &a{1}"
  }
  saturation {
    fixed = "&2Saturation &a{0}"
    range = "&2Saturation &a{0} - &a{1}"
  }
  starvation {
    fixed = "&2Starvation &a{0}"
    range = "&2Starvation &a{0} - &a{1}"
  }
  attack-range {
    fixed = "&2Attack Range &a{0}"
    range = "&2Attack Range &a{0} - &a{1}"
  }
  max-health {
    fixed = "&cMax Health &a{0}"
    range = "&cMax Health &a{0} - &a{1}"
  }
  life-steal-rate {
    fixed = "&cLife Steal Rate &a{0}"
    range = "&cLife Steal Rate &a{0} - &a{1}"
  }
  life-steal {
    fixed = "&cLife Steal Amount &a{0}"
    range = "&cLife Steal Amount &a{0} - &a{1}"
  }
  loot-rate {
    fixed = "&dLoot Rate &a{0}"
    range = "&dLoot Rate &a{0} - &a{1}"
  }
  loot-immune {
    value = "&dImmune to Loot Rate"
  }
}
commands {
  range {
    clear-attribute = "Cleared {1} attribute for {0}"
    append-attribute = "Added {1} attribute for {0}"
    prepend-attribute = "Added {1} attribute for {0}"
  }
  marker {
    mark-attribute = "Marked {1} attribute for {0}"
    unmark-attribute = "Unmarked {1} attribute for {0}"
  }
  possess {
    mark-attribute = "Marked that the item is possessed by {0}"
    unmark-attribute = "Marked that the item is publicized"
  }
  init {
    already-exist = "The item has already been initialized"
    succeed = "Successfully initialized the item, in which lore texts will be taken over control"
  }
  drop {
    nonexist = "Please make sure that the command is executed by a player in whose hand an item is initialized"
    succeed = "Successfully dropped all the data from the item and set the item as uninitialized"
    warning-ok = "&c&nI UNDERSTAND THE CONSEQUENCES AND I AM SURE TO CONTINUE"
    warning = [
      "&eATTENTION! The operation is going to clear all the data and it cannot be recovered"
      "&eAre you sure to continue? {0}"
    ]
  }
  args {
    not-a-number = "{0} is not either a number or a percentage"
    range-consistency = "The values to be parsed should be either two numbers or two percentages"
  }
}